{
	"name": "service",
	"version": "0.1.0",
	"type": "module",
	"private": true,
	"scripts": {
		"check": "eslint . --fix --cache && tsc --noEmit",
		"synth": "cdk synth",
		"diff": "cdk diff",
		"deploy": "cdk deploy --require-approval never",
		"lint": "eslint . --fix --cache"
	},
	"dependencies": {
		"aws-cdk-lib": "^2.250.0",
		"constructs": "^10.6.0"
	},
	"devDependencies": {
		"aws-cdk": "^2.1118.2",
		"tsx": "^4.21.0",
		"typescript": "*"
	}
}
