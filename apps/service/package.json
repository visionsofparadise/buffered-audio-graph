{
	"name": "service",
	"version": "0.1.0",
	"type": "module",
	"private": true,
	"license": "MIT",
	"scripts": {
		"fix": "eslint . --fix --cache",
		"check": "concurrently --group \"eslint . --no-cache -f unix\" \"tsc --noEmit --pretty false\"",
		"synth": "cdk synth",
		"diff": "cdk diff",
		"deploy": "cdk deploy --require-approval never"
	},
	"dependencies": {
		"aws-cdk-lib": "^2.262.1",
		"constructs": "^10.6.0"
	},
	"devDependencies": {
		"aws-cdk": "^2.1133.0",
		"tsx": "^4.21.0",
		"typescript": "*"
	}
}
