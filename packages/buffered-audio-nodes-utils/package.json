{
	"name": "@buffered-audio/utils",
	"version": "0.8.1",
	"type": "module",
	"exports": {
		".": {
			"import": "./dist/index.js",
			"types": "./dist/index.d.ts"
		}
	},
	"files": [
		"dist"
	],
	"sideEffects": false,
	"publishConfig": {
		"access": "public"
	},
	"author": "Matt Cavender",
	"license": "ISC",
	"repository": {
		"type": "git",
		"url": "https://github.com/visionsofparadise/buffered-audio-graph",
		"directory": "packages/buffered-audio-nodes-utils"
	},
	"scripts": {
		"check": "eslint . --fix --cache && tsc --noEmit",
		"lint": "eslint . --fix --cache",
		"build": "tsup",
		"unit": "vitest run --project unit",
		"integration": "vitest run --project integration"
	},
	"devDependencies": {
		"@types/node": "^25.3.5",
		"tsup": "^8.0.0",
		"typescript": "*",
		"vitest": "^3.0.0"
	},
	"dependencies": {
		"@buffered-audio/core": "0.12.0"
	}
}
